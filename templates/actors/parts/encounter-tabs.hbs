{{!-- Sheet Tab Navigation --}}
<nav class="sheet-navigation tabs" data-group="primary">    
    {{#if (isNotLimited @root)}}
        <a class="item" data-tab="stats">{{localize "COG.tabs.stats"}}</a>
    {{/if}}
    <a class="item" data-tab="description">{{localize "COG.tabs.description"}}</a>
    {{#if (isNotLimited @root)}}
        <a class="item" data-tab="effects">{{ localize "COG.tabs.effects" }}</a> 
    {{/if}}
</nav>
{{!-- Sheet Body --}}
<section class="sheet-body">
    {{#if (isNotLimited @root)}}
        <div class="tab stats" data-group="primary" data-tab="stats">
            {{> "systems/cog/templates/actors/parts/stats/encounter-stats.hbs"}}
        </div>
    {{/if}}
    <div class="tab description" data-group="primary" data-tab="description">
        {{> "systems/cog/templates/actors/parts/actor-description.hbs"}}
    </div>
    {{#if (isNotLimited @root)}}
        <div class="tab effects" data-group="primary" data-tab="effects">
            {{> "systems/cog/templates/effects/effects.hbs"}}
        </div>
    {{/if}}
</section>